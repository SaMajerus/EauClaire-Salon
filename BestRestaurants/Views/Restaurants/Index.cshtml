@{
  Layout = "_Layout";
}

@using BestRestaurants.Models;
@model List<BestRestaurants.Models.Restaurant>;

<h1>Your Reviewed Restaurants</h1>

@if (@Model.Count == 0)
{
  <h3>
    You haven't reviewed any restaurants yet!
  </h3>
}

<ul>
  @foreach(Restaurant restaurant in Model)
  {
    <li>@Html.ActionLink($"{restaurant.Name}", "Show", new { id = restaurant.RestaurantId }) - @restaurant.Cuisine.CuisineName</li>
  }
</ul>

<p>
  @Html.ActionLink("Review a Restaurant", "Create")
</p>
<p>
  @Html.ActionLink("See all food types", "Index", "Cuisines")
</p>


